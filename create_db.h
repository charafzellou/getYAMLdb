#include <stdio.h>
#include <stdlib.h>
#include <windows.h>
#include <string.h>
/*
void create_db(){

    //int valid;
    //char *dirname;

    printf("Enter a directory path to be created : ((C:/name_chosen)");
    gets(dirname);
    valid = mkdir(dirname);

    if(!valid){
        printf("Directory created\n");
    }

    else{
        printf("Couldn't create directory\n");
        exit(1);
        getch();
        system("dir/p");
        getch();


        //Créer la Database en .yaml

        printf("Directory will be created by default in repertory\n");
        char name_db;

        printf("Name your DATABASE YAML file : \n");
        scanf("%s", &name_db);

        FILE *fp = NULL;

        fp = fopen(name_db, "wr+");
        if(fp == NULL){
            printf("Error in opening file ! \n");
        }

    }
*/
